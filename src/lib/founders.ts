/**
 * Platform founders shown in the "Team members" section on the landing page.
 *
 * List the X handles (without @, lowercase) of the people to feature. Their
 * card — display name, avatar, @handle — is resolved live from their linked X
 * account and profile picture, so updating a profile picture updates the card.
 */
export const PLATFORM_FOUNDER_X_HANDLES: string[] = ["asuzu_a"];
